# 05-tasks.md — 진행 체크리스트

## 진행 규칙

> 아래 규칙을 위반하면 작업을 중단하고 원인을 해결한 뒤 재진행한다.

1. **순서 엄수** — Phase·단계 번호 순서대로 진행. 건너뜀 금지.
2. **단계별 검증 필수** — 검증 방법을 실제로 실행해 통과를 확인한 후에만 완료(✅) 체크.
3. **테스트 동반** — 각 기능 단계는 테스트와 함께 완료한다(절대규칙 3).
4. **안전 우선** — 실전 주문 경로는 가드 테스트가 통과하기 전까지 활성화하지 않는다.

상태 표기: ⬜ 미착수 / 🟡 진행중 / ✅ 완료

---

## Phase 0 — 설계 문서 (현재)

| # | 단계 | 검증 | 상태 |
|---|------|------|------|
| 1 | 00-overview 작성 | 핵심 결정·읽는 순서 포함 | ✅ |
| 2 | 01-product 작성 | MVP 기능 4개·안전 제약 포함 | ✅ |
| 3 | 02-specs 작성 | 스택·KIS API 목록·DB·env·테스트 포함 | ✅ |
| 4 | 03-design 작성 | 레이아웃·색상·실시간 흐름 포함 | ✅ |
| 5 | 04-conventions 작성 | 네이밍·폴더·git·보안 포함 | ✅ |
| 6 | 05-tasks 작성 | Phase별 체크리스트 | ✅ |

---

## Phase 1 — 프로젝트 골격 + 인증

목표: 서버가 뜨고 KIS 토큰 발급까지 동작.

| # | 단계 | 검증 | 상태 |
|---|------|------|------|
| 1 | backend 골격 (FastAPI, pyproject, config) | `GET /health` → `{"status":"ok","mode":"paper"}` 200 확인 | ✅ |
| 2 | `.env.example` + 설정 로드 + `.gitignore` | `.env`/`Key/`/`data/` git-ignore 검증 통과 | ✅ |
| 3 | DB 초기화 (스키마 5개, WAL) | 부팅 시 테이블 5개 생성 + WAL 확인 | ✅ |
| 4 | KIS 인증 클라이언트 (토큰 발급/캐시/갱신) | mock 테스트 3건 통과 + **실제 모의투자 토큰 발급 확인** | ✅ |
| 5 | 모드 분리 (paper/live 도메인·TR_ID 전환) | 모드별 도메인/TR_ID 단위 테스트 통과 | ✅ |

> 검증: `pytest` 9건 전부 통과, `/health` 200, 모의투자 실서버 토큰 발급 성공(len 346).

---

## Phase 2 — 시세 모니터링

목표: 관심종목 실시간 시세를 대시보드에서 확인.

| # | 단계 | 검증 | 상태 |
|---|------|------|------|
| 1 | 관심종목 CRUD API + DB | CRUD 서비스 테스트 + 라우트(201/409/404) 검증 | ✅ |
| 2 | KIS REST 시세 조회 (현재가) | mock 테스트 2건 + **실서버 현재가 조회 성공** | ✅ |
| 3 | KIS WebSocket 구독 (체결가) + 재연결 | approval_key·체결 파싱·구독 메시지 테스트 통과 (지수 백오프 재연결 구현, 라이브 연결은 장중 통합검증 대기) | ✅ |
| 4 | 대시보드 WebSocket 푸시 | 허브 브로드캐스트/끊긴 클라이언트 제거 테스트 + `/ws/quotes` 등록·해제 검증 | ✅ |
| 5 | 프론트: 관심종목·실시간 시세표 | Vitest 5건 통과 + `vite build` 성공 | ✅ |

> 검증: 백엔드 `pytest` 22건 + 프론트 `vitest` 5건 전부 통과. TestClient로 watchlist CRUD + `/api/quotes/005930` 실서버 200 + `/ws/quotes` 연결 등록(0→1→0) 확인. `vite build` 성공(200KB/63KB gzip).
> React 대시보드: 모드 배너 + 관심종목 CRUD + 실시간 시세표(REST 스냅샷 + WS 병합) + 시세수집 시작/정지. Vite 프록시로 `/api`·`/ws` → 백엔드 :8000.
> 차트(분봉)·호가창은 후속 보강. 봇/시세수집은 안전상 기본 OFF(`/api/market/start`로 수동 시작).

---

## Phase 3 — 전략 엔진 (룰 기반)

목표: 시세에서 매매 신호를 생성(주문과 분리).

| # | 단계 | 검증 | 상태 |
|---|------|------|------|
| 1 | 지표 계산 유틸 (이동평균·RSI, pandas) | 지표 값 단위 테스트 통과 | ✅ |
| 2 | 이동평균 크로스 전략 | 골든/데드 크로스 신호 테스트 통과 | ✅ |
| 3 | RSI 전략 | 과매수/과매도 신호 테스트 통과 | ✅ |
| 4 | 전략 설정 API + 종목별 on/off·한도 | 설정 CRUD 테스트 + 라우트(201/400/404) 검증 | ✅ |
| 5 | 신호 로그 저장 + 대시보드 표시 | 신호 저장/조회 테스트 + 프론트 SignalLog 표시 | ✅ |

> 검증: 백엔드 `pytest` 35건 + 프론트 `vitest` 7건 전부 통과. 전략 CRUD·신호 라우트 TestClient 종단 확인, `vite build` 성공.
> 전략은 **신호 생성만** 담당(주문 집행은 Phase 4). 신호→실시간 푸시(WS)와 시세→전략 평가 연동도 Phase 4에서 봇 엔진에 통합.

---

## Phase 4 — 자동 주문 집행 (모의투자)

목표: 신호를 검증 후 모의 주문으로 집행.

| # | 단계 | 검증 | 상태 |
|---|------|------|------|
| 1 | 안전가드 (일일/종목 한도·수량) | 한도 초과 거부 테스트 6건 통과 | ✅ |
| 2 | 봇 시작/정지 토글 (기본 OFF) | 봇 OFF 시 주문 차단 테스트 통과 | ✅ |
| 3 | 잔고 검증 + 주식주문(현금) | 신호→가드→모의주문 흐름 테스트(mock) + KIS 매수주문 mock 통과 | ✅ |
| 4 | 포지션·잔고 갱신 | 로컬 포지션 계산 + KIS 잔고조회(mock) 테스트 통과 | ✅ |
| 5 | 주문 취소 (order-rvsecncl) | 취소 API mock 테스트 통과 | ✅ |
| 6 | 감사 로그 (봇·주문·신호·리스크) | 로그 기록 테스트 + RISK/ORDER 카테고리 검증 | ✅ |

> 검증: 백엔드 `pytest` **50건** 전부 통과. 봇 파이프라인(시세→전략→신호→가드→모의주문) 3종 시나리오(집행/OFF무시/가드거부) 테스트. TestClient로 `/api/bot/status`·`/api/orders`·`/api/positions`·`/api/audit` 확인(봇 기본 OFF).
> **후속(Phase 5/보강)**: KIS 매수가능조회(`inquire-psbl-order`) 사전 점검, 실시간 체결통보(`H0STCNI`, AES-CBC) WS 수신, 주문 정정(취소와 동일 API). 현재 포지션은 KIS `inquire-balance` 기준으로 조회하고, 로컬 주문 체결 상태는 `inquire-daily-ccld` 결과로 보정한다.

---

## Phase 5 — 대시보드 통합 + 운영

목표: 모니터링·제어를 한 화면에서. 운영 안정화.

| # | 단계 | 검증 | 상태 |
|---|------|------|------|
| 1 | 모드 배너 + 봇 토글 + 연결 상태 | ModeBanner·BotControl 테스트 통과 | ✅ |
| 2 | 포지션·손익 표 | PositionTable 렌더(평단·현재가·평가금액·손익·손익률) | ✅ |
| 3 | 신호/주문/시스템 로그 뷰어 | SignalLog·OrderLog·AuditLogView 추가 | ✅ |
| 4 | 실전 전환 확인 절차 (env + 대시보드 확인) | 백엔드 live-가드 409 테스트 + 프론트 2단계 확인 테스트 통과 | ✅ |
| 5 | 전체 테스트 + 문서 동기화 | 백엔드 52 + 프론트 10 전부 통과, docs 갱신, `.gitattributes` 추가 | ✅ |

> 검증: 백엔드 `pytest` **52건** + 프론트 `vitest` **10건** 전부 통과, `vite build` 성공.
> 실전 게이트: 백엔드 `/api/bot/start`는 `mode=live`에서 `confirm_live` 없으면 409 `LIVE_CONFIRM_REQUIRED`. 프론트 BotControl은 실전 시 2단계 확인 후 `confirm_live=true` 전송.

---

## Phase 6 — 후속 보강 (미착수)

목표: MVP에서 "후속 보강"으로 미룬 항목을 단계적으로 구현. **각 항목은 독립적이며 우선순위에 따라 선택 진행한다.**

> Phase 1~5(MVP)는 완료 상태다. 아래 항목들은 02-specs/01-product에 명세돼 있으나
> MVP 범위 밖으로 미뤄둔 보강 기능이다. `constants.py`에 TR_ID만 선언돼 있고 호출
> 로직은 미구현 상태다.

| # | 단계 | 검증 | 상태 |
|---|------|------|------|
| 1 | 분봉/일봉 차트 (`inquire-time-itemchartprice` 등) | REST 차트 조회 + 프론트 차트 렌더 | ✅ (Phase 9) |
| 2 | 호가창 10단계 (`inquire-asking-price`, `H0STASP0`) | 호가 조회/구독 테스트 + 프론트 호가표 | ⬜ |
| 3 | 매수가능조회 사전 점검 (`inquire-psbl-order`) | 주문 전 매수가능 금액/수량 확인 + 가드 연동 테스트 | ⬜ |
| 4 | 실시간 체결통보 (`H0STCNI0`, AES-CBC 복호) | 체결 수신 파싱 테스트 + 포지션 즉시 갱신 | ⬜ |
| 5 | 주문 정정 (`order-rvsecncl`, 정정 구분 01) | 정정 API mock 테스트 | ⬜ |
| 6 | 종목별 금액 한도 내 자동 수량 산정 | `max_amount` 기반 매수 수량 계산 테스트 | ⬜ |

> 참고: 현재 종목별 한도(`max_qty`/`max_amount`)는 **초과 시 거부**(01-product 명세대로)로
> 동작한다. 6번은 "한도 내에서 최대 수량을 자동 매수"하도록 확장하는 별도 보강 항목이다.

---

## Phase 7 — 분야별 추천 종목 (설계 단계)

목표: 분야(KRX 테마)별로 종목을 복합 점수로 랭킹해 추천하는 새 페이지. **설계: `06-recommend.md`.**

> 방향 확정: **하이브리드**(로컬 .mst 필터 → 후보만 실시간 KIS API). 분류=**KRX 테마**,
> 점수=**복합 점수**(모멘텀 40 / 펀더멘털 35 / 수급 25). 추천은 정보 제공으로 자동매매와 분리.

| # | 단계 | 검증 | 상태 |
|---|------|------|------|
| 0 | `06-recommend.md` 설계 문서 + 미결 결정 확정 | 분류·점수·파이프라인·API·화면·안전 + §9 확정 | ✅ |
| 1 | .mst 테마/재무 파싱 (`stocks/sector.py` 신규) | 테마 분류·ROE·재무 파싱 테스트 7건 | ✅ |
| 2 | 복합 점수 서비스 (정규화·가중합·degrade) | 점수 계산 단위 테스트 6건 | ✅ |
| 3 | KIS 수급 API 래퍼 (`kis/rankings.py`, inquire-investor) | mock 응답 파싱·degrade 테스트 3건 | ✅ |
| 4 | 추천 라우터 (`/api/recommend/themes`, `/{theme}`) | TestClient 종단(200/404/정렬) 4건 | ✅ |
| 5 | 프론트 추천 페이지 + 테마 선택 + 카드 (탭) | Vitest 5건 + tsc + vite build 통과 | ✅ |
| 6 | 추천→관심종목 수동 연동 + 면책 표기 | 관심종목 추가 버튼·면책 문구 테스트 | ✅ |
| 7 | 카드 현재가·등락률 + 종목 클릭→차트(기존 ChartModal 재사용) | RecommendPage 신규 3건(현재가·클릭 onSelect(symbol, name)·버튼 분리) + 전체 44건 통과 | ✅ (feat/recommend-page) |

> 검증: 백엔드 `pytest` **86건** 전부 통과(기존 66 + 신규 20). 파서는 실데이터로 오프셋
> 검증(SK하이닉스=반도체, 현대차=자동차, ROE/기준일 정확). 모멘텀은 종목별 `inquire-price`,
> 수급은 `inquire-investor`로 후보(N=30)만 조회 후 복합점수 랭킹(K=10).
> 프론트(#5·#6)는 병렬 진행 중인 프론트 작업과의 충돌을 피해 **맨 마지막**에 진행.

---

## Phase 8 — 계좌 잔고 + 칸막이 설정가능금액 (완료)

목표: 대시보드에서 KIS 실계좌 잔고(예수금·주문가능현금·총평가·평가손익·순자산)를 확인하고,
자본 칸막이 패널에 계좌 주문가능현금 기준 **설정가능금액**을 표시한다.

> 배경: 주문내역/포지션/칸막이는 있었으나 **계좌 예수금·주문가능현금을 볼 곳이 없었다.**
> KIS 주식잔고조회 `output2`를 노출하는 신규 엔드포인트 + 패널을 추가했다.

| # | 단계 | 검증 | 상태 |
|---|------|------|------|
| 1 | `get_account_summary()` (output2 파싱) + `/api/account/balance` 라우터(graceful) | 파싱·dict허용·엔드포인트·KIS오류 graceful 4건 | ✅ |
| 2 | 프론트 `AccountPanel` + 폴링 연동 + 스타일 | Vitest 4건 + tsc + vite build 통과 | ✅ |
| 3 | 칸막이 패널 **설정가능금액** 표기(주문가능현금 − Σ칸막이 가용액, 표시+경고만) | Vitest 3건 + tsc + build 통과 | ✅ |
| 4 | 잔고 지연 개선 — 토큰 프리워밍(기동) + 로딩 상태 분리("불러오는 중…") | 백엔드 2건 + 프론트 로딩 테스트 + build | ✅ |

> 검증: 백엔드 `pytest` 신규 4건 추가(전체 90건 통과), 프론트 `vitest` 신규 7건 추가(전체 33건 통과).
> 라이브: 토큰 갱신 후 모의계좌 실데이터(예수금 10,000,000원) 정상 표시 확인. KIS 토큰
> 만료(`EGW00121`)·모의서버 500 시 `available=false`로 graceful 동작 확인.
> #3은 자본 칸막이↔계좌 현금 **하드 인터락 없음**(01-product 명시)을 보완하는 표시용 가이드.
> 설정가능금액 시각 검증은 Playwright 보류(사용자 병렬 작업) — 단위/빌드로 확인.

---

## Phase 9 — 종목 차트 (일봉/주봉/분봉 모달, 완료)

목표: 관심종목 클릭 → 캔들차트 모달(일봉/주봉/분봉 토글). 설계: `docs/07-chart.md`.
(Phase 6 #1 "분봉/일봉 차트"를 충족한다.)

> 결정: 일봉/주봉/분봉 토글 · **lightweight-charts** 도입(사용자 승인) · 모달 팝업.

| # | 단계 | 검증 | 상태 |
|---|------|------|------|
| 1 | `kis/charts.py`(일봉/주봉/분봉 파싱·graceful) + `/api/charts/{symbol}` 라우터 | 일봉·분봉·graceful·엔드포인트·interval검증 5건 | ✅ |
| 2 | `ChartModal`(lightweight-charts, 일봉/주봉/분봉 토글·닫기) + WatchList 클릭 연동 | Vitest 8건(모듈 모킹) + tsc + build | ✅ |
| 3 | 차트 모달 지연 로딩(코드 스플리팅)으로 초기 번들 분리 | 빌드 청크 분리 확인(초기 68KB gzip 복귀) | ✅ |

> 검증: 백엔드 `pytest` 신규 5건(전체 98건), 프론트 `vitest` 신규 8건(전체 41건) 통과.
> lightweight-charts 5.2.0(v5 `addSeries` API). 차트 청크는 모달 첫 오픈 시에만 로드.
> 분봉 시각은 KST 벽시계를 UTC로 환산해 축 라벨을 KST로 표기. KIS 오류 시 빈 캔들 graceful.
> 브라우저 시각 검증은 Playwright 보류(사용자 병렬 작업) — 단위/빌드로 확인.

---

## Phase 10 — 안전 가드 보강(미보유 매도 억제 + 일일 한도 UI)

목표: 모의투자 로그 분석에서 드러난 문제 해결.
(1) 봇이 보유하지 않은 종목에 매도 신호를 내 거절 주문이 폭증(000660: SELL 270건 전부 거절).
(2) 일일 주문 한도(20건)가 낮아 봇이 즉시 멈춤(1,528/1,548 주문 거절).
(3) 한도를 코드 상수가 아니라 대시보드에서 확인·변경하고 싶음.

> 결정: 일일 한도를 `risk_limit` 테이블(모드별)에 저장 → 런타임 변경. 기본값 100건으로 상향.
> 매도가능 0이면 봇이 신호 단계에서 주문을 만들지 않음. 매도 거절 사유 분리(미보유/초과).

| # | 단계 | 검증 | 상태 |
|---|------|------|------|
| 1 | 미보유 매도 억제(`trading_bot`): 매도가능 0이면 주문 미생성 | pytest 1건(거절 BUY만 남고 SELL 없음) | ✅ |
| 2 | 일일 한도 기본 100건(`config`/`.env`) + 매도 사유 분리(`risk_guard` NO_BOT_HOLDING/SELL_EXCEEDS_HOLDING) | pytest 갱신 + 매도가능 헬퍼 | ✅ |
| 3 | 한도 DB화 + 조회/변경 API(`risk_limit` 테이블, `risk_limit_service`, `/api/risk-limits`) | pytest 9건(폴백·왕복·사용량·엔드포인트) | ✅ |
| 4 | 대시보드 한도 바(`RiskLimitBar`, 전략 위) — 사용량 표시 + 변경 시 재확인 | vitest 5건 + tsc | ✅ |
| 5 | 칸막이 **수정** 모달 +버튼 상한 누락 버그 수정(추가 폼과 동일하게 `설정가능+현재원금`으로 고정) | vitest 1건 | ✅ |
| 6 | 전략 수정 모달에서 **다른 전략 종류로 교체** 지원(`onEditStrategy`: 새 전략 추가 후 기존 삭제, 칸막이 보존) + **주문가능현금 아래 칸막이 합계** 표시 | vitest 2건 | ✅ |
| 7 | **거절 주문 비저장**(가드 거절은 orders에 안 남기고 RISK 감사로그만) + **신호 중복 억제**(확정봉 평가 `closed_ticks` + 봇 직전 방향 디바운스) + 기존 거절 주문 2,591건 DB 정리(백업 후) | pytest 신규 2건(`closed_ticks`·중복억제) + 기존 2건 갱신 | ✅ |
| 8 | **칸막이 한도 = 원금 + min(0, 실현손익)** — 실현이익은 한도 미반영(보수적), 손실만 축소(안전). `budget_service.effective_ceiling` + UI 문구·문서 정합 | pytest 신규 2건(`effective_ceiling`·손실축소) + 기존 3건 갱신 | ✅ |

> 검증: 백엔드 pytest 전체 231건, 프론트 vitest 전체 131건 통과.
> 일일 한도는 매수·매도를 함께 집계한다(side 구분 없음).
> 칸막이가 주문가능현금만큼 차면 설정가능이 0/음수가 되어 +스테퍼가 멈추는 것은 의도된 동작(초과 배정 방지). 칸막이 합계 표시로 원인을 바로 확인할 수 있다.
> ⚠️ A(거절 비저장)·C(신호 억제)는 **백엔드 재시작 후** 적용된다. 재시작 전까지 구버전 봇이 거절 주문을 계속 생성할 수 있다.
> ⚠️ 잔여: 일일 주문 **금액** 한도 기본값은 100만원 유지 — 1주 230만원대 종목(000660)은
> 금액 한도에 막히므로, 대시보드 한도 바에서 금액 한도를 올려야 매수 가능.

---

## Phase 11 — 기간별 매매손익 탭

목표: KIS HTS [0856] 기간별매매손익 화면에 대응하는 새 탭(분야별 추천 옆). 조회기간(시작~종료)
+ 조회 버튼, 매도 건별 실현손익 표 + 매도/매수/합계 요약.

> 결정(하이브리드): 실전=KIS API `TTTC8715R`(실제 수수료·세금), 모의=로컬 주문 이력 계산
> (수수료·세금 추정). KIS 기간별손익 API는 모의 미지원이라 모드별로 소스를 나눈다.

| # | 단계 | 검증 | 상태 |
|---|------|------|------|
| 1 | 로컬 계산 `trade_pnl_service`(평균원가·추정 수수료/세금·요약) + `/api/trade-pnl` | pytest 8건 | ✅ |
| 2 | 실전 KIS `TTTC8715R` 연동(`kis/trade_pnl`, TR_ID 추가) + 라우터 하이브리드(graceful) | pytest respx 1건 | ✅ |
| 3 | 프론트 `TradePnlPage`(조회기간·정렬·표·요약·추정 안내) + 3번째 탭 | vitest 3건 + tsc | ✅ |

| 4 | 봇/직접 분류 표기(`source`, 구분 컬럼) — 실전 KIS 매매를 우리 orders와 (종목,날짜) 대조 | pytest 2건(annotate·local source) + vitest | ✅ |

> 검증: 백엔드 pytest +11, 프론트 vitest +3 통과.
> ⚠️ `TTTC8708R`(기간별손익일별합산, 일별)은 TR_ID·seam만 추가 — 일별/월별 뷰는 추후.
> ⚠️ 미완: **모의(paper)에서 봇 외(직접) 매매 표시** — 로컬은 봇 주문만 보유. KIS 당일체결
> (VTTC0081R, 모의 지원) 기간 연동이 필요(paper 기간 지원 확인 후 진행). 실전은 KIS가
> 계좌 전체를 돌려주므로 직접 매매도 포함되며 봇/직접으로 분류됨.

## Phase 12 — 신호·시스템로그 모드 분리

목표: `signals`·`audit_logs`가 `mode` 컬럼이 없어 모의/실전이 섞이던 결함 수정(orders 등은
이미 모드 분리). 저장·조회·API·프론트 전 구간을 모드별로 격리.

| # | 단계 | 검증 | 상태 |
|---|------|------|------|
| 1 | 스키마 `mode` 컬럼 추가(signals·audit_logs) + 마이그레이션(기존 행 'paper') | init_db 멱등 | ✅ |
| 2 | `signal_service`·`audit_service` mode 저장/필터 + 봇·라우터 호출부 mode 전달 | pytest 2건(모드 분리) | ✅ |
| 3 | `/api/signals`·`/api/audit` mode 파라미터 + 프론트 `getSignals/getAudit`·App 폴링 viewMode | 전체 회귀 | ✅ |

> 검증: 백엔드 pytest +2(전체 244), 프론트 134 통과. 실 DB 마이그레이션 적용 완료.

## Phase 13 — 실전 거버너(완충층) Phase A

목표: 신호↔주문 사이에 완충층을 넣어 횡보장 왕복매매를 줄인다. 상세 설계는
[11-strategy.md](11-strategy.md) "실전 거버너" 절. **기본값은 현행 호환(중립), UI 폼엔
추천값**을 채워 보여준다.

| # | 단계 | 검증 | 상태 |
|---|------|------|------|
| 1 | 틱사이즈 유틸 이식(`market_rules.tick_size`) | pytest(가격대별) | ✅ |
| 2 | ma_cross 필터: `confirm_bars`·`diff_buffer_ticks`·`trend_ma`·`use_long_slope`(역호환) | pytest 5건 | ✅ |
| 3 | rsi_ma 필터: `confirm_bars`·`ma_buffer_ticks`(히스테리시스)·`max_distance_ticks`(역호환) | pytest(역호환) | ✅ |
| 4 | 봇 거버너: `min_hold_bars`·`cooldown_bars`·미체결매수 중복방지 상태머신 | pytest 3건 | ✅ |
| 5 | `bar_ticks` 상한 200→2000 + UI 파라미터화(폼 노출) + **추천 기본값** | vitest + tsc | ✅ |

> 검증: 백엔드 pytest +8(전체 254), 프론트 vitest 135 통과. 중립 기본값으로 기존 동작 보존.
> Phase B(손절/익절/trailing·하루 손실 한도·시간 필터), Phase C(스프레드·거래대금·변동성
> 필터·호가 구독)는 후속.

## Phase 14 — 실전 거버너 Phase B(손절·트레일링 + 하루 손실 + 시간 필터)

| # | 단계 | 검증 | 상태 |
|---|------|------|------|
| 1 | 보호 청산: `stop_loss_ticks`·`take_profit_ticks`·`trailing_stop_ticks`(최소보유/쿨다운 무시, 즉시) — 봇 `_check_stops` + 진입가/최고가 추적 | pytest(stop_exit_reason·손절 보호청산) | ✅ |
| 2 | 하루 손실 한도: `risk_limit.max_daily_loss`(모드별) + 당일 실현손익 초과 시 신규 매수 차단 | pytest(하루손실 매수차단) | ✅ |
| 3 | 시간 필터(전역 설정): 장 시작 후/마감 전 N분 신규 진입 금지 | pytest(in_entry_block_window) | ✅ |

| 4 | 프론트 노출: 전략 폼에 stop_loss/trailing/take_profit 틱 + RiskLimitBar 하루 손실(실현손익 표시·편집) + 도움말 예시·폭 확대 + 도움말/차트 모달 X로만 닫기 | vitest 138 + tsc | ✅ |

> 검증: 백엔드 pytest 258, 프론트 vitest 138. 기본값 0(off)이면 현행 동작 보존.
> 도움말에 상황별 예시(매수/매도/노이즈) 추가, 도움말 폭 640px, 도움말·차트·전략수정·칸막이수정
> 모달은 X(또는 취소/저장) 버튼으로만 닫힌다(실수 닫힘 방지).

## Phase 15 — 실전 거버너 Phase C(변동성·거래대금 필터)

| # | 단계 | 검증 | 상태 |
|---|------|------|------|
| 1 | 변동성 필터 `min_volatility_ticks`: 최근 ~20봉 (고가−저가)가 기준 미만이면 매수 보류 | pytest(recent_range_ticks·게이트) | ✅ |
| 2 | 거래대금 필터 `min_turnover`: 틱 `volume`(누적) 추적 → 최근 ~20봉 거래대금 기준 미만이면 매수 보류 | pytest(recent_turnover·게이트) | ✅ |
| 3 | UI 노출(전략 폼 파라미터, 기본 0=off — 종목별 튜닝 필요) | vitest + tsc | ✅ |

> 검증: 백엔드 pytest +3(전체 261), 프론트 vitest 138. 기본 0(off)이면 현행 동작 보존.
> ⚠️ **스프레드 필터는 보류** — 봇이 호가(H0STASP0)를 수집하지 않음. 실시간 호가 구독
> 플러밍이 선행돼야 함(별도 작업).

## Phase 16 — 스프레드 필터(호가 구독)

| # | 단계 | 검증 | 상태 |
|---|------|------|------|
| 1 | 실시간 호가 H0STASP0 구독·파싱(`parse_orderbook`, 최우선 매도/매수호가) + 체결가와 동시 구독 | pytest(호가 파싱) | ✅ |
| 2 | 봇이 종목별 스프레드 갱신(호가 메시지는 봇 전용, 대시보드 미브로드캐스트) | pytest(스프레드 갱신) | ✅ |
| 3 | `max_spread_ticks` 매수 게이트 + UI 파라미터(기본 0=off) | pytest(스프레드 게이트) + vitest | ✅ |

> 검증: 백엔드 pytest +3(전체 264), 프론트 vitest 138. Phase C의 보류 항목(스프레드) 해소.
> 거버너 Phase A·B·C 전 항목 완료.

## Phase 17 — OFF 전략 관찰 신호(로그 전용)

| # | 단계 | 검증 | 상태 |
|---|------|------|------|
| 1 | `signals.observe` 컬럼 추가 + 마이그레이션, `save_signal(observe=)`·`list_signals` 반영 | pytest(관찰 플래그 저장) | ✅ |
| 2 | 봇이 `enabled=False` 전략도 신호 평가→`_observe_signal`로 로그만 기록(실주문·거버너·보호청산 제외) | pytest(OFF 전략 관찰 신호) | ✅ |
| 3 | 대시보드 매매 신호 표에 `관찰` 배지 표시 | vitest(관찰 배지) | ✅ |
| 4 | 11-strategy.md '방식 B' 문서화 | 문서 동기화 | ✅ |

> 목적: 검토 문서 '방식 B' — 기본 실주문은 `rsi_ma`, `ma_cross`는 OFF로 두고 신호만 관찰·검증.
> 검증: 백엔드 pytest +2(전체 266), 프론트 vitest +2(SignalLog). 봇 OFF(정지) 시에는 관찰도 미동작.

## Phase 18 — 전략 추천 기본값 안정형 교체

| # | 단계 | 검증 | 상태 |
|---|------|------|------|
| 1 | `STRATEGY_DEFAULTS` 안정형 세트: rsi_ma(rsi 21·high 75·ma 80·min_hold 8·cd 15), ma_cross(short 10·long 40·buffer 2·min_hold 8·cd 15) | vitest(기본값·도움말) | ✅ |
| 2 | 도움말 문구(워밍업·예시) + 11-strategy 파라미터 표/거버너 추천값 동기화 | 문서 동기화 | ✅ |

> 근거: 검토 문서(`Reference/SStock_strategy_parameter_review.md`) 안정형 1차 추천(잦은 매매·노이즈
> 둔감화). `_HISTORY_SIZE=6000` 유지 전제로 `bar_ticks`는 50 중심. 엔진 중립 기본값(역호환)은
> 불변이며, **UI '새로 추가' 시 채워지는 추천 프리필만** 변경(기존 적용 전략엔 영향 없음).
> 검증: 프론트 vitest 140(기존 단정 6건을 새 기본값으로 갱신), 백엔드 무관(프론트 전용 변경).

## 진행 현황 요약

| Phase | 단계 | 완료 | 진행률 |
|-------|------|------|-------|
| Phase 0 — 설계 문서 | 6 | 6 | 100% ✅ |
| Phase 1 — 골격+인증 | 5 | 5 | 100% ✅ |
| Phase 2 — 시세 모니터링 | 5 | 5 | 100% ✅ |
| Phase 3 — 전략 엔진 | 5 | 5 | 100% ✅ |
| Phase 4 — 자동 주문(모의) | 6 | 6 | 100% ✅ |
| Phase 5 — 대시보드+운영 | 5 | 5 | 100% ✅ |
| **MVP 소계 (0~5)** | **32** | **32** | **100% 🎉** |
| Phase 6 — 후속 보강 | 6 | 1 | 17% ⬜ (#1 차트는 Phase 9에서 완료, 나머지 선택 진행) |
| Phase 7 — 분야별 추천 종목 | 6 | 6 | 100% ✅ (코드리뷰 반영 완료) |
| Phase 8 — 계좌 잔고 + 칸막이 설정가능금액 | 4 | 4 | 100% ✅ |
| Phase 9 — 종목 차트(일봉/주봉/분봉 모달) | 3 | 3 | 100% ✅ |
| Phase 10 — 안전 가드 보강(미보유 매도 억제 + 일일 한도 UI) | 4 | 4 | 100% ✅ |

> MVP(Phase 0~5)는 완료. Phase 6은 선택적 보강 항목. Phase 7은 백엔드+프론트 구현 완료
> (백엔드 pytest 93건 + 프론트 vitest 26건 통과). 코드리뷰 반영 완료: 동시성 제한(세마포어5)
> + TTL 캐시(300초), .mst 마지막 줄 개행 견고성, 동점 순위 평균 정규화, 모멘텀 문서 정합.
> 남은 LOW(response_model 스키마, 세부 가중치 상수화)는 선택 보강.
